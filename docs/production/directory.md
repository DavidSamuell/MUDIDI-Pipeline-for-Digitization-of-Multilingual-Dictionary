# Directory input

Place one image or PDF per page in a directory. Numeric page stems determine ordering.

```bash
uv run mudidi run --config examples/configs/production/directory-inference.yaml
```

The canonical configuration is included directly below:

```yaml
--8<-- "examples/configs/production/directory-inference.yaml"
```
