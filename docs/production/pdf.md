# PDF input

PDF mode selects 1-based dictionary and introduction pages from one source scan. Install `pdftk` before running it.

```bash
uv run mudidi run --config examples/configs/production/pdf-inference.yaml
```

```yaml
--8<-- "examples/configs/production/pdf-inference.yaml"
```

