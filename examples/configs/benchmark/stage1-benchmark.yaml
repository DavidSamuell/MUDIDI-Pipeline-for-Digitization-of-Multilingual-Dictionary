version: 1
kind: benchmark_run

input:
  dataset_dir: ../../../dataset/MUDIDI/dictionaries

output:
  directory: ../../../outputs/benchmark/stage-1

pipeline:
  stage: "1"
  stage1_mode: flat

models:
  default: gemini/gemini-3-flash-preview

runtime:
  experiment_name: gemini3flash_flat
  one_page_per_entry: true
