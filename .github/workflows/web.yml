name: Local web application

on:
  workflow_dispatch:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v6
        with:
          enable-cache: true
      - run: uv sync --extra dev --extra web
      - name: Unit and integration coverage
        run: >-
          uv run pytest
          tests/web
          tests/execution/test_contracts.py
          tests/execution/test_approved_pass2.py
          --cov=mudidi.web
          --cov-report=term-missing
          --cov-fail-under=80
      - name: Install Chromium
        run: uv run playwright install --with-deps chromium
      - name: Browser production journey
        run: uv run pytest tests/e2e/test_local_web_journey.py
      - name: Build distributable wheel
        run: uv build
